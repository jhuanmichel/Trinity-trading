# Crypto Agent — Dependências
# Instale com: pip install -r requirements.txt

ccxt>=4.3.0               # Conexão com exchanges (MEXC, Binance, etc.)
pandas>=2.0.0             # Manipulação de dados
ta>=0.10.0                # Indicadores técnicos (RSI, MACD, Bollinger, etc.)
anthropic>=0.30.0         # Claude API (cérebro da IA)
python-telegram-bot>=20.0 # Alertas Telegram
requests>=2.31.0          # HTTP para APIs externas
python-dotenv>=1.0.0      # Carregar .env
schedule>=1.2.0           # Agendamento de tarefas
numpy>=1.24.0             # Dependência do pandas/ta
reportlab>=4.0.0          # PDF (Morning Brief, Cycle Intelligence)
Pillow>=10.0.0            # Redimensionar imagens curadas nos PDFs
yfinance>=0.2.40          # Dados macro: correlação cripto
fastapi>=0.111.0          # Dashboard web — servidor API
uvicorn>=0.29.0           # Dashboard web — servidor ASGI
websockets>=12.0          # WebSocket Binance liquidações em tempo real
feedparser>=6.0.0         # Parse de feeds RSS (Geopolitical Engine)
